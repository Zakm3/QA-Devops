const {shuffleArray} = require('./utils')

describe('shuffleArray should', () => {
test("shuffleArray should return same length as argument sent in", () => {
    expect(shuffleArray.length).toEqual([]);
})
})